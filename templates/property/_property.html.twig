<div class="card mb-4">
    <div class="card-body">
        <h3 class="card-title">
            <a href="{{path('app_property_show', {slug: property.slug, id:property.id})}}">
                {{property.title}}
            </a>
        </h3>
        <p class="card-text">{{property.city}} ({{property.postalCode}})</p>
        <div class="text-primary" style="font-size:2rem; font-weight: bold;">
            {{property.formatedPrice}} €
        </div>
    </div>
</div>
